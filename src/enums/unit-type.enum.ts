// Define an enum for UnitType
export enum UnitType {
  PCS = 'pieces',
  KG = 'kilogram',
  YARD = 'yard',
  LITER = 'liter',
  BOX = 'box',
}

// Define a default unit type
export const DEFAULT_UNIT: UnitType = UnitType.PCS;
